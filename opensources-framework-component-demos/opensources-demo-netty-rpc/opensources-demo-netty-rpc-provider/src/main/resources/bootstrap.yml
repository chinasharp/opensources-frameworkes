opensourceframework.log.version: 1.0.0-SNAPSHOT
opensourceframework.env.logger.level: INFO
opensourceframework.env.module: opensources-demo-netty-rpc-provider
opensourceframework.profiles.active: dev
opensourceframework.server.port: 8080


server:
  port: ${opensourceframework.server.port}

spring:
  profiles:
    active: ${opensourceframework.profiles.active}
  application:
    name: ${opensourceframework.env.module}
  main:
    allow-bean-definition-overriding: true
